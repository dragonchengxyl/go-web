apiVersion: v1
kind: Service
metadata:
  name: studio-backend
  namespace: studio
  labels:
    app: studio-backend
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: studio-backend
