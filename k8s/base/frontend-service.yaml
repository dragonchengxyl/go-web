apiVersion: v1
kind: Service
metadata:
  name: studio-frontend
  namespace: studio
  labels:
    app: studio-frontend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: studio-frontend
